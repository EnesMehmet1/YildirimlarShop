@using YildirimlarE.Models
@model List<Urun>
<div class="padding">
    <h4>En popüler 10 Ürünümüz</h4>
    <ul class="list list-pad">
        @{int y = 0;}
        @foreach (var x in Model)
        {
            <li class="list-bg">@{y++;}<span>@y.</span><a href="#">@x.Ad</a></li>
        }
    </ul>
    <a href="#" class="link1">See all hotels</a>
</div>
