@model YildirimlarE.Models.BirlesikTablolar
@{
    ViewBag.Title = "UrunDetay";
    Layout = "~/Views/Shared/_MainLayout.cshtml";
}

<head>
    <title>Personal Blog a Blogging Category Flat Bootstarp  Responsive Website Template | Single :: w3layouts</title>
    <link href="~/web2/css/bootstrap.css" rel='stylesheet' type='text/css' />
    <link href="~/web2/css/style.css" rel='stylesheet' type='text/css' />
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
    <meta name="keywords" content="Personal Blog Responsive web template, Bootstrap Web Templates, Flat Web Templates, Andriod Compatible web template,
	Smartphone Compatible web template, free webdesigns for Nokia, Samsung, LG, SonyErricsson, Motorola web design" />
    <script type="application/x-javascript"> addEventListener("load", function() { setTimeout(hideURLbar, 0); }, false); function hideURLbar(){ window.scrollTo(0,1); } </script>
    <!----webfonts---->
    <link href='http://fonts.googleapis.com/css?family=Oswald:100,400,300,700' rel='stylesheet' type='text/css'>
    <link href='http://fonts.googleapis.com/css?family=Lato:100,300,400,700,900,300italic' rel='stylesheet' type='text/css'>
    <!----//webfonts---->
    <script src="http://ajax.googleapis.com/ajax/libs/jquery/2.1.1/jquery.min.js"></script>
    <!--end slider -->
    <!--script-->
    <script type="text/javascript" src="js/move-top.js"></script>
    <script type="text/javascript" src="js/easing.js"></script>
    <!--/script-->
    <!---->

</head>
    <div class="single">
        <div class="container">
            <div class="col-md-8 single-main">
                <div class="single-grid">
                    @foreach (var x in Model.u)
                    {
                        <img src="@x.FotoUrl" alt="" width="700" , height="500" />
                        <h2>@x.Ad</h2>
                        <p>
                            @x.UrunAciklama <br />
                        </p>
                    }
                </div>
                @foreach (var x in Model.uy)
                {
                    <ul class="comment-list">
                        <li>

                            <img src="~/web2/images/avatar.png" class="img-responsive" alt="">
                            <div class="desc">
                                <p>@x.KullaniciAdi :<a href="#" title="Posts by admin" rel="author">@x.Yorum</a></p>
                            </div>
                            <div class="clearfix"></div>

                        </li>
                    </ul>
                }

                @Html.Action("YorumYapP", "Urun");
            </div>

            <div class="col-md-4 side-content">
                <div class="recent">
                    <h3>Ürünler</h3>
                    <ul>
                        @foreach (var x in Model.u2)
                        {
                            <li><a href="/Urun/UrunDetay/@x.UrunId">@x.Ad </a><a href="#"></a></li>
                        }
                    </ul>
                </div>
                <div class="comments">
                    <h3>Mağzalar</h3>
                    @foreach (var x in Model.m2)
                    {
                        <ul>
                            <li><a href="/Magza/MagzaDetay/@x.MagzaId">@x.MagzaAdi</a><a href="#"></a></li>
                        </ul>
                    }
                </div>
                <div class="clearfix"></div>
                <div class="clearfix"></div>
            </div>
            <div class="clearfix"></div>
        </div>
    </div>







